{
  "name": "@chat/centrifugo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.mjs"
  },
  "types": "./dist/index.d.mts",
  "scripts": {
    "build": "pnpm clean && tsdown index.ts --minify --splitting --sourcemap --treeshake",
    "clean": "rimraf dist",
    "fix": "biome fix",
    "format": "biome format",
    "lint": "biome lint"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@types/node": "catalog:",
    "rimraf": "^6.1.3",
    "tsdown": "^0.22.9",
    "typescript": "catalog:"
  },
  "peerDependencies": {
    "typescript": "catalog:"
  }
}
